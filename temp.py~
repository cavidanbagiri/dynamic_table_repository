
def averager():
    total = 0.0
    count = 0
    def inner(value):
        nonlocal total, count
        total += value
        count +=1
        return total / count

    return inner

cavidan = averager()
print(cavidan(10))
print(cavidan(30))
print(cavidan(50))
